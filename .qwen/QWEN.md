
## Workflow Rules

- Do not give up because `git diff` returns nothing.  Always check for staged local files when I ask you to review.

- When flagging a diff change, quote the exact old->new text from `git diff` before claiming a change exists.  Never assert a line changed without showing it.  If you can't show the diff, you likely hallucinated the claim - retract it.
